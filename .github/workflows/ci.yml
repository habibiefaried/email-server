name: CI

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: emailuser
          POSTGRES_PASSWORD: emailpass
          POSTGRES_DB: emaildb
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Cache Go modules
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Download dependencies
        run: go mod download

      - name: Run unit tests
        run: go test ./... -v -race -coverprofile=coverage.out

      - name: Build email server
        run: go build -o email-server ./cmd/email-server

      - name: Start email server in background
        env:
          DB_URL: "user=emailuser password=emailpass dbname=emaildb host=localhost port=5432 sslmode=disable"
          HTTP_PORT: "48080"
        run: |
          ./email-server &
          SERVER_PID=$!
          echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
          
          # Wait for server to be ready
          echo "Waiting for email server to start..."
          for i in {1..30}; do
            if curl -s http://localhost:48080/ > /dev/null; then
              echo "Email server is ready!"
              break
            fi
            echo "Attempt $i/30: Server not ready yet..."
            sleep 1
          done
          
          # Verify server is running
          if ! curl -s http://localhost:48080/ > /dev/null; then
            echo "Failed to start email server"
            exit 1
          fi

      - name: Install swaks (SMTP testing tool)
        run: sudo apt-get update && sudo apt-get install -y swaks

      - name: Load sample data via SMTP
        run: |
          echo "Loading gmail.txt sample..."
          swaks --to admin@test.milahabibie.com \
                --from habibie.faried@paidy.com \
                --server localhost:2525 \
                --data samples/gmail.txt \
                || echo "Failed to send gmail.txt"
          
          echo "Loading anonymousemail.txt sample..."
          swaks --to admin2@test.milahabibie.com \
                --from noreply@anonymousemail.se \
                --server localhost:2525 \
                --data samples/anonymousemail.txt \
                || echo "Failed to send anonymousemail.txt"
          
          echo "Loading attachments.txt sample..."
          swaks --to admin@test.milahabibie.com \
                --from habibie.faried@paidy.com \
                --server localhost:2525 \
                --data samples/attachments.txt \
                || echo "Failed to send attachments.txt"
          
          # Wait for emails to be processed
          sleep 2
          echo "Sample data loaded!"

      - name: Generate and send custom test emails
        run: |
          echo "Generating 5 custom test emails..."
          
          # Email 1: Simple text email
          swaks --to testuser@example.com \
                --from sender1@test.com \
                --server localhost:2525 \
                --header "Subject: Test Email 1 - Simple Text" \
                --body "This is a simple test email generated by CI pipeline." \
                || echo "Failed to send email 1"
          
          # Email 2: Email with HTML content
          swaks --to testuser@example.com \
                --from sender2@test.com \
                --server localhost:2525 \
                --header "Subject: Test Email 2 - HTML Content" \
                --body "<html><body><h1>Hello from CI</h1><p>This is an HTML email.</p></body></html>" \
                --add-header "Content-Type: text/html" \
                || echo "Failed to send email 2"
          
          # Email 3: Email with special characters
          swaks --to testuser@example.com \
                --from sender3@test.com \
                --server localhost:2525 \
                --header "Subject: Test Email 3 - Special Chars: 日本語 🎉" \
                --body "Testing special characters: áéíóú ñ 中文 日本語 한글 🚀 ✅ 💯" \
                || echo "Failed to send email 3"
          
          # Email 4: Email to different recipient
          swaks --to anotheruser@example.com \
                --from automated@ci.test \
                --server localhost:2525 \
                --header "Subject: CI Test Email 4 - Different Recipient" \
                --body "This email is sent to a different recipient for testing isolation." \
                || echo "Failed to send email 4"
          
          # Email 5: Email with long body
          swaks --to testuser@example.com \
                --from bulk@sender.com \
                --server localhost:2525 \
                --header "Subject: Test Email 5 - Long Content" \
                --body "$(printf 'Line %d: This is a test email with a longer body to verify the system handles larger content properly.\n' {1..50})" \
                || echo "Failed to send email 5"
          
          # Wait for emails to be processed
          sleep 2
          echo "✓ Generated and sent 5 custom test emails!"

      - name: Test API - Health check
        run: |
          echo "Testing health check endpoint..."
          response=$(curl -s http://localhost:48080/)
          if [ "$response" = "OK" ]; then
            echo "✓ Health check passed"
          else
            echo "✗ Health check failed: $response"
            exit 1
          fi

      - name: Test API - Valid address with data
        run: |
          echo "Testing valid address: admin@test.milahabibie.com (should have 2 emails)..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com")
          count=$(echo "$response" | jq '. | length')
          
          if [ "$count" -eq 2 ]; then
            echo "✓ Found $count emails for admin@test.milahabibie.com"
          else
            echo "✗ Expected 2 emails, got $count"
            echo "Response: $response"
            exit 1
          fi
          
          # Verify email structure
          if echo "$response" | jq -e '.[0] | has("id", "from", "to", "subject", "body", "created_at", "attachments")' > /dev/null; then
            echo "✓ Email structure is valid"
          else
            echo "✗ Email structure is invalid"
            exit 1
          fi

      - name: Test API - Another valid address
        run: |
          echo "Testing valid address: admin2@test.milahabibie.com (should have 1 email)..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin2@test.milahabibie.com")
          count=$(echo "$response" | jq '. | length')
          
          if [ "$count" -eq 1 ]; then
            echo "✓ Found $count email for admin2@test.milahabibie.com"
          else
            echo "✗ Expected 1 email, got $count"
            echo "Response: $response"
            exit 1
          fi

      - name: Test API - Address not found
        run: |
          echo "Testing non-existent address: notfound@example.com..."
          response=$(curl -s "http://localhost:48080/inbox?name=notfound@example.com")
          count=$(echo "$response" | jq '. | length')
          
          if [ "$count" -eq 0 ]; then
            echo "✓ Correctly returned empty array for non-existent address"
          else
            echo "✗ Expected empty array, got $count emails"
            echo "Response: $response"
            exit 1
          fi

      - name: Test API - Missing required parameter
        run: |
          echo "Testing missing 'name' parameter..."
          http_code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:48080/inbox")
          
          if [ "$http_code" -eq 400 ]; then
            echo "✓ Correctly returned 400 Bad Request"
          else
            echo "✗ Expected 400, got $http_code"
            exit 1
          fi

      - name: Test API - Pagination with limit
        run: |
          echo "Testing pagination with limit=1..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com&limit=1")
          count=$(echo "$response" | jq '. | length')
          
          if [ "$count" -eq 1 ]; then
            echo "✓ Limit parameter works correctly"
          else
            echo "✗ Expected 1 email with limit=1, got $count"
            echo "Response: $response"
            exit 1
          fi

      - name: Test API - Pagination with offset
        run: |
          echo "Testing pagination with offset=1..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com&limit=10&offset=1")
          count=$(echo "$response" | jq '. | length')
          
          if [ "$count" -eq 1 ]; then
            echo "✓ Offset parameter works correctly (got remaining 1 email)"
          else
            echo "✗ Expected 1 email with offset=1, got $count"
            echo "Response: $response"
            exit 1
          fi

      - name: Test API - Invalid limit parameter
        run: |
          echo "Testing invalid limit parameter (non-numeric)..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com&limit=invalid")
          count=$(echo "$response" | jq '. | length')
          
          # Should fall back to default limit (100), so should return all 2 emails
          if [ "$count" -eq 2 ]; then
            echo "✓ Invalid limit parameter falls back to default"
          else
            echo "✗ Expected 2 emails with invalid limit, got $count"
            exit 1
          fi

      - name: Test API - Negative offset
        run: |
          echo "Testing negative offset (should be ignored/default to 0)..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com&offset=-5")
          count=$(echo "$response" | jq '. | length')
          
          # Should fall back to offset 0 and return all emails
          if [ "$count" -eq 2 ]; then
            echo "✓ Negative offset handled correctly"
          else
            echo "✗ Expected 2 emails with negative offset, got $count"
            exit 1
          fi

      - name: Test API - Limit exceeds maximum
        run: |
          echo "Testing limit exceeds maximum (2000 > 1000)..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com&limit=2000")
          count=$(echo "$response" | jq '. | length')
          
          # Should cap at max 1000 or return available emails (2)
          if [ "$count" -eq 2 ]; then
            echo "✓ Excessive limit handled correctly"
          else
            echo "✗ Expected 2 emails with excessive limit, got $count"
            exit 1
          fi

      - name: Test API - Attachment metadata
        run: |
          echo "Testing attachment metadata in response..."
          response=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com")
          
          # Check if any email has attachments
          has_attachments=$(echo "$response" | jq '[.[] | select(.attachments | length > 0)] | length')
          
          if [ "$has_attachments" -gt 0 ]; then
            echo "✓ Found $has_attachments email(s) with attachments"
            
            # Verify attachment structure
            if echo "$response" | jq -e '[.[] | select(.attachments | length > 0)][0].attachments[0] | has("id", "filename", "content_type", "size")' > /dev/null; then
              echo "✓ Attachment metadata structure is valid"
            else
              echo "✗ Attachment metadata structure is invalid"
              exit 1
            fi
          else
            echo "⚠ No attachments found (this might be expected)"
          fi

      - name: Test API - Empty email address
        run: |
          echo "Testing empty email address..."
          http_code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:48080/inbox?name=")
          
          if [ "$http_code" -eq 400 ]; then
            echo "✓ Correctly returned 400 for empty address"
          else
            echo "✗ Expected 400, got $http_code"
            exit 1
          fi

      - name: Test API - CORS headers
        run: |
          echo "Testing CORS headers..."
          headers=$(curl -s -I "http://localhost:48080/inbox?name=admin@test.milahabibie.com")
          
          if echo "$headers" | grep -qi "Access-Control-Allow-Origin"; then
            echo "✓ CORS headers present"
          else
            echo "✗ CORS headers missing"
            exit 1
          fi

      - name: Test API - Custom generated emails for testuser@example.com
        run: |
          echo "Testing custom generated emails: testuser@example.com (should have 4 emails)..."
          response=$(curl -s "http://localhost:48080/inbox?name=testuser@example.com")
          count=$(echo "$response" | jq '. | length')
          
          if [ "$count" -eq 4 ]; then
            echo "✓ Found $count custom emails for testuser@example.com"
          else
            echo "✗ Expected 4 emails, got $count"
            echo "Response: $response"
            exit 1
          fi
          
          # Verify subjects are present
          subjects=$(echo "$response" | jq -r '.[].subject')
          echo "Subjects found:"
          echo "$subjects"

      - name: Test API - Custom email for different recipient
        run: |
          echo "Testing custom email for anotheruser@example.com (should have 1 email)..."
          response=$(curl -s "http://localhost:48080/inbox?name=anotheruser@example.com")
          count=$(echo "$response" | jq '. | length')
          
          if [ "$count" -eq 1 ]; then
            echo "✓ Found $count email for anotheruser@example.com"
          else
            echo "✗ Expected 1 email, got $count"
            echo "Response: $response"
            exit 1
          fi
          
          # Verify it's the correct email
          subject=$(echo "$response" | jq -r '.[0].subject')
          if echo "$subject" | grep -q "Different Recipient"; then
            echo "✓ Correct email received: $subject"
          else
            echo "✗ Unexpected subject: $subject"
            exit 1
          fi

      - name: Test API - Special characters handling
        run: |
          echo "Testing special characters in subject..."
          response=$(curl -s "http://localhost:48080/inbox?name=testuser@example.com")
          
          # Check if special characters are preserved
          special_email=$(echo "$response" | jq -r '.[] | select(.subject | contains("Special Chars"))')
          
          if [ ! -z "$special_email" ]; then
            subject=$(echo "$special_email" | jq -r '.subject')
            body=$(echo "$special_email" | jq -r '.body')
            echo "✓ Special character email found"
            echo "  Subject: $subject"
            echo "  Body preview: ${body:0:100}"
          else
            echo "✗ Special character email not found"
            exit 1
          fi

      - name: Test API - Long content handling
        run: |
          echo "Testing long content email..."
          response=$(curl -s "http://localhost:48080/inbox?name=testuser@example.com")
          
          # Find the long content email
          long_email=$(echo "$response" | jq '.[] | select(.subject | contains("Long Content"))')
          
          if [ ! -z "$long_email" ]; then
            body_length=$(echo "$long_email" | jq -r '.body' | wc -c)
            echo "✓ Long content email found (body length: $body_length bytes)"
            
            if [ "$body_length" -gt 1000 ]; then
              echo "✓ Long body stored correctly"
            else
              echo "⚠ Body seems shorter than expected: $body_length bytes"
            fi
          else
            echo "✗ Long content email not found"
            exit 1
          fi

      - name: Test API - Total email count across all addresses
        run: |
          echo "Verifying total email count..."
          
          # Count emails for all test addresses
          count1=$(curl -s "http://localhost:48080/inbox?name=admin@test.milahabibie.com" | jq '. | length')
          count2=$(curl -s "http://localhost:48080/inbox?name=admin2@test.milahabibie.com" | jq '. | length')
          count3=$(curl -s "http://localhost:48080/inbox?name=testuser@example.com" | jq '. | length')
          count4=$(curl -s "http://localhost:48080/inbox?name=anotheruser@example.com" | jq '. | length')
          
          total=$((count1 + count2 + count3 + count4))
          expected=8  # 2 + 1 + 4 + 1
          
          echo "Email counts:"
          echo "  admin@test.milahabibie.com: $count1"
          echo "  admin2@test.milahabibie.com: $count2"
          echo "  testuser@example.com: $count3"
          echo "  anotheruser@example.com: $count4"
          echo "  Total: $total"
          
          if [ "$total" -eq "$expected" ]; then
            echo "✓ Total email count matches expected: $expected"
          else
            echo "✗ Expected $expected total emails, got $total"
            exit 1
          fi

      - name: Upload coverage report
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.out
          flags: unittests
          name: codecov-umbrella
        continue-on-error: true

      - name: Cleanup
        if: always()
        run: |
          if [ ! -z "$SERVER_PID" ]; then
            kill $SERVER_PID || true
          fi
